<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.studio.tis_3.com.keepy">

    <application

        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_keppy"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_keppy"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CadProduto" />
        <activity android:name=".ListarProduto" />
        <activity android:name=".ListarCliente" />
        <activity android:name=".CadastroCliente" />
        <activity android:name=".telaPagamento" />
        <activity android:name=".telaRelatorio" />
        <activity android:name=".telaVendas"></activity>
    </application>

</manifest>